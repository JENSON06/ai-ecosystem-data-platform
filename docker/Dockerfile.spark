FROM bitnami/spark:3.5.0

USER root

# Install Python dependencies into the Spark image
# so every worker node has the same libraries available
COPY requirements.txt /tmp/requirements.txt
RUN pip install --no-cache-dir -r /tmp/requirements.txt

USER 1001
